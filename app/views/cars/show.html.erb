<div class="container">
  <div class="row justify-content-center">
    <div class="col-sm-6 col-sm-offset-3">
      <h1 class="text-center">Book a car</h1>

      <%= render @car, show: true %>

      <%= render 'bookings/form', car: @car, booking: @booking, submit: 'Book' %>
    </div>
  </div>
</div>
